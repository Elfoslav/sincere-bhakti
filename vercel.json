{
	"framework": "nextjs",
	"buildCommand": "pnpm test && prisma generate && next build",
	"installCommand": "pnpm install",
	"git": {
		"deploymentEnabled": {
			"*": false,
			"main": true,
			"develop": true
		}
	}
}
